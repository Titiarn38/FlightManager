@model Vols.Models.Ville

@{
    ViewData["Title"] = "Delete";
}

<div class="container">
    <h1 class="display-4">Supprimer la Ville</h1>

    <h3>Êtes-vous sûr de vouloir supprimer ceci ?</h3>
    <div>
        <h4>Ville</h4>
        <hr />
        <dl class="row">
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.IdVille)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.IdVille)
            </dd>
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.Nom)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.Nom)
            </dd>
        </dl>

        <form asp-action="Delete">
            <div class="form-group">
                <button type="submit" class="btn btn-danger">Supprimer</button> |
                <a class="btn btn-secondary" asp-action="Index">Retour à la liste</a>
            </div>
        </form>
    </div>
</div>
